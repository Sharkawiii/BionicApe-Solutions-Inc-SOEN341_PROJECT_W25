<h1>This is admin page</h1>

<body>
    <h1>List of Users</h1>
    <ul>
        <% users.forEach(user=> { %>
            <li>
                <a href="/editUser?username=<%= user.username %>"> <%= user.username %> by <%= user.role %> </a>
            </li>
            <% }); %>
    </ul>
</body>  